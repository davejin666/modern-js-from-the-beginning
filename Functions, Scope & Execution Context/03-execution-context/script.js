// Run the following code in the browser's debugger to step through each line of the code.
const x = 100;
const y = 50;

function getSum(n1, n2) {
    let sum = n1 + n2;
    return sum;
}

const sum1 = getSum(x, y);
const sum2 = getSum(10, 5);
